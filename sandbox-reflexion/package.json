{
  "name": "sandbox-reflexion",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 4173 --directory ..",
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/browser-check.mjs"
  }
}
